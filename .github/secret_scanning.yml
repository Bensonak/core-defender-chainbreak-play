paths-ignore:
  # This immutable compiled bundle was verified to contain only the Firebase Web
  # App client key. Firebase web keys are public client configuration, and this
  # key is blocked from the Generative Language API. Keep this exclusion exact
  # so every future bundle remains subject to secret scanning.
  - "classic/assets/index-DqqFq53_.js"
